# Payment Table Generator

A Streamlit app to generate clean table images for customer payments.

### Features
- Multiple customers
- Indian currency format
- Download table as PNG
- Mobile friendly (works with web-to-app wrappers)

### Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
